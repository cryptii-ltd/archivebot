[auth]
clientsecret= # Bot auth token

[storage]
servers= #IPs of remote/local mariadb message storage servers

[database]
localdb_ip= # Local mariadb ip, for keeping track of archives and keys
localdb_name= # Local mariadb name, this must be created first.

mariadb_user=
mariadb_password=
mariadb_port=
mariadb_name= # Remote mariadb name, this must be created first.